# 本地网页浏览器尺寸配置文件
# 配置格式：key=value，值为有效的CSS尺寸值（像素、百分比等）
# 以下是所有支持的配置项及其说明：

# 窗口默认宽度 - 应用启动时的初始宽度
# 默认值：1200px
# 建议：根据屏幕分辨率调整，建议在1024px以上
windowWidth=1400

# 窗口默认高度 - 应用启动时的初始高度
# 默认值：800px
# 建议：根据屏幕分辨率调整，建议在768px以上
windowHeight=800

# 标题栏高度 - 顶部标题栏的高度
# 默认值：30px
# 建议：使用适中的高度，确保标题和按钮显示正常
titleBarHeight=30

# 按钮尺寸 - 窗口控制按钮的宽度和高度
# 默认值：24px
# 建议：使用正方形尺寸，确保按钮点击区域合适
buttonSize=24

# 按钮间距 - 窗口控制按钮之间的间距
# 默认值：8px
# 建议：使用适当的间距，避免按钮过于拥挤或分散
buttonGap=8

# 边框圆角 - 元素的圆角半径（如标题栏、按钮等）
# 默认值：50%（圆形按钮）
# 建议：根据设计风格调整，方形设计可使用较小的圆角值
borderRadius=50%

# 内边距 - 元素内部的间距
# 默认值：8px
# 建议：使用适当的内边距，确保内容不拥挤
padding=8px

# 外边距 - 元素外部的间距
# 默认值：0px
# 建议：根据布局需求调整
margin=0px

# 配置示例：
# windowWidth=1024
# windowHeight=768
# titleBarHeight=28
# buttonSize=20
# buttonGap=6
# borderRadius=4px
# padding=6px
# margin=2px

# 注意事项：
# 1. 尺寸值必须是有效的CSS尺寸格式
# 2. 窗口尺寸只支持像素值，不支持百分比
# 3. 每行只能有一个配置项
# 4. 注释行以#开头
# 5. 配置项名称区分大小写
# 6. 等号前后的空格会被自动忽略
# 7. 空行会被忽略
# 8. 重启应用程序后配置生效