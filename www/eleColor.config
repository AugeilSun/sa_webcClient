# 本地网页浏览器主题颜色配置文件
# 配置格式：key=value，值为有效的CSS颜色值（十六进制、RGB、RGBA等）
# 以下是所有支持的配置项及其说明：

# 主色调 - 用于body背景色、标题栏背景色
# 默认值：#f0e6ff（淡紫色）
# 建议：使用柔和的背景色，避免过于鲜艳影响阅读
primaryColor=#e0f7fa

# 次要色调 - 用于标题栏底部边框、按钮颜色、加载指示器背景色
# 默认值：#9370db（中紫色）
# 建议：使用与主色调协调的颜色，增强视觉层次感
secondaryColor=#00acc1

# 强调色 - 用于文本颜色、按钮颜色
# 默认值：#6a5acd（深紫色）
# 建议：使用比主色调更深的颜色，确保文本可读性
accentColor=#00838f

# 文本颜色 - 用于标题文本、提示文本
# 默认值：#6a5acd（深紫色）
# 建议：使用高对比度颜色，确保文本清晰可读
textColor=#006064

# 悬停颜色 - 用于按钮悬停时的背景色
# 默认值：rgba(147, 112, 219, 0.8)（半透明紫色）
# 建议：使用与主色调相关的半透明颜色，提供良好的交互反馈
hoverColor=rgba(0, 172, 193, 0.8)

# 配置示例：
# primaryColor=#f5f5f5
# secondaryColor=#2196f3
# accentColor=#1976d2
# textColor=#333333
# hoverColor=rgba(33, 150, 243, 0.8)

# 注意事项：
# 1. 颜色值必须是有效的CSS颜色格式
# 2. 每行只能有一个配置项
# 3. 注释行以#开头
# 4. 配置项名称区分大小写
# 5. 等号前后的空格会被自动忽略
# 6. 空行会被忽略
# 7. 重启应用程序后配置生效